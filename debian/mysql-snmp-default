# Defaults for mysql-snmp initscript
# sourced by /etc/init.d/mysql-snmp
# installed at /etc/default/mysql-snmp by the maintainer scripts

#
# This is a POSIX shell fragment
#

# Additional options that are passed to the Daemon.
DAEMON_OPTS=""
